# 🌐 Malla Interactiva ICF

Una visualización interactiva de la malla curricular de ICF (versión 2023, desactualizada).

Este proyecto fue creado con [Create React App](https://github.com/facebook/create-react-app) y está desplegado en Vercel.

---

## 🚀 Ver la Aplicación en Vivo

No necesitas instalar nada para ver el proyecto. Puedes acceder directamente a la versión en vivo haciendo clic aquí:

**[https://malla-interactiva-icf.vercel.app/](https://malla-interactiva-icf.vercel.app/)**

---

## 🛠️ Para Desarrolladores (Correr Localmente)

Si estás interesado en el código fuente o quieres correr el proyecto en tu propia máquina, puedes seguir estos pasos.

### 1. Prerrequisitos

Asegúrate de tener [Node.js](https://nodejs.org/) (que incluye npm) instalado en tu sistema.

### 2. Instalación y Ejecución

1.  **Clona el repositorio:**
    ```bash
    git clone [https://github.com/iache-d/malla-interactiva-icf.git](https://github.com/iache-d/malla-interactiva-icf.git)
    ```

2.  **Navega al directorio del proyecto:**
    ```bash
    cd malla-interactiva-icf
    ```

3.  **Instala las dependencias:**
    Este comando lee el archivo `package.json` e instala todas las librerías necesarias (como React).
    ```bash
    npm install
    ```

4.  **Inicia el servidor de desarrollo:**
    Este es el comando de tu archivo `arranq.txt`.
    ```bash
    npm start
    ```

Esto correrá la aplicación en modo de desarrollo y la abrirá automáticamente en tu navegador en [http://localhost:3000](http://localhost:3000).

---

## 📜 Scripts Disponibles

* **`npm start`**: Corre la app en modo de desarrollo.
* **`npm run build`**: Crea la versión de producción en la carpeta `build/`.
* **`npm test`**: Ejecuta las pruebas.
* **`npm run eject`**: (Solo usuarios avanzados) Expulsa la app de la configuración de Create React App.
